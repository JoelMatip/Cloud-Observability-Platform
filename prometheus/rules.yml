groups:
- name: example
  rules:
  - alert: HighCPUUsage
    expr: process_cpu_seconds_total > 100
    for: 1m
    labels:
      severity: warning
    annotations:
      summary: "High CPU usage detected"